import React from 'react'

const PageLoadingHandler = ({ message }) => {
    return (
        <div className="container">
            <p>{message}</p>
        </div>
    )
}

export default PageLoadingHandler
